v0.0.3 - 2026-09-20

优化：

- 关闭当前标签时，先回到它打开的标签，再找同一来源的标签，再回到来源标签，最后找相邻标签；同一档都优先左侧，切换或换页不会忘记来源。
- 获得对应业务「导出」权限后，可以在各类表格中框选单元格并用 Ctrl+C（Mac 为 Cmd+C）复制；没有框选时复制当前聚焦的格子，勾选行仍用于批量操作。
- 表格打开详情改为双击；也可聚焦数据格后按 Enter。报表里的项目链接同样改为双击，行内操作按钮仍点一次即可。
- 报销列表和审核工作台在项目名称旁增加项目编号，可按编号排序、筛选和查找候选。
- 项目选择候选改为编号在前、名称在后；当前值旁可一键复制「编号,名称」，即使当前不能编辑也可复制。
- 精简附件预览组件，减少应用更新的下载量，保留常用文档、图片、文本和音视频的预览能力。
- 培训录像的播放器菜单里去掉了下载项，与全应用没有下载入口保持一致；倍速、全屏、投屏照常可用。
- 所有列表默认改为最新的排在最上面，打开页面不用再手动点一次倒序。客户、项目、合同、招投标、报销、用车公出、公司制度、考勤，以及审核台的待审和历史记录都是如此。
- 项目详情里的收款记录和归档材料也改为最新的排在最上面。
- 合同列表的默认顺序按合同编号从大到小排，不按内部序号。内部序号是从老系统两张合同表合并过来的，按它排会把早年的专项债合同排在近期的普通合同前面。
- 员工目录和餐补名册仍然按姓名排序，方便找人。
- 安卓端可以正常使用：顶部导航不再被系统状态栏压住，键盘弹出时也不会挡住正在填写的输入框。
- 安卓端的应用图标换成了博华 ERP 自己的图标，之前显示的是打包工具的默认图标。
- 手机等窄屏下换了一套导航：左上角按钮打开导航抽屉，模块和它下面的页面都在里面，点模块右侧的箭头可以直接展开二级页面跳过去。
- 标签页在窄屏下照常显示，可以左右滑动；最左侧新增一个带数量的按钮，点开能看到全部已打开的标签并直接切换或关闭。
- 触摸屏上的按钮和点击区域整体放大，手指点按不容易点错；用鼠标时保持原来的紧凑尺寸不变。
- 表单在窄屏下自动改为单列，客户、报销、私车公用、培训、公司制度等页面不再需要左右拖动才能填完。
- 执行地这类多级选择在窄屏下改为一次选一级，顶部显示已选路径，点路径可以退回上一级重选；只选到省或只选到市仍然是有效的填法。
- 培训在窄屏下可以从列表进入详情并返回，返回后保留列表筛选条件，详情加载中也能返回。
- 弹窗长内容改为正文滚动，标题和底部操作保持可见；窗口高度连标题和按钮都容不下时，仍可滚动整个弹窗访问操作。
- 版本更新提示在电脑上变宽，更新说明单独一块滚动，更新按钮和下载进度条固定在底部，下载过程中一直看得见。
- 项目详情里的「进度」改名为「前期进度」，新增入口、编辑弹窗与空状态使用相同名称。
- 客户联系人弹窗的添加、取消操作保持可见；批量操作和表单的拦截摘要留在操作区旁，长清单统一在正文滚动。
- 项目详情的「收款」页签移到「归档」前面，顺序改为前期进度、招投标、合同、收款、归档，与实际业务先后一致。
- 报表模块的「项目报表」改名为「一般项目报表」，与「专项债报表」更好区分。
- 一般项目报表和专项债报表的项目名称列默认固定在左侧，横向滚动时不会跟着滚走；不需要的话在列菜单里取消固定即可，取消后会记住。
- 招投标去掉了「经办人」字段。它不影响谁看得到或谁能审核这条记录，也没有派工、通知、工作量统计用到它，指派了不产生任何效果。
- 招投标的标段结果新增「未开标」选项，和「中标」「未中标」并列。原先要回到未开标状态得点输入框右侧的清除按钮，不容易发现。
- 新建招投标时「技术方案制作人」改为必填。已有记录里这一格为空的不受影响，编辑时不必补填；但填过内容之后不能再清空。
- 列表筛选的候选清单换了一套：清单上方多了搜索框，输入后到服务端去整列的全部取值里找，不再只在已经列出来的那几十项里筛。
- 候选清单滚到底会自动接着加载，不需要点「加载更多」；下方会注明「已列出 N 项，继续滚动或搜索可看更多」。
- 取值很多的列现在也有候选清单可勾了，以前这些列只有一个文本输入框：项目名称、项目编号、客户名称、合同编号、招标项目名称、招投标的项目编号、报销单号、打卡地址、制度名称、登录名。
- 报表里的项目进度、前期进度、投标文件制作人、技术方案制作人、问题阶段、可研进度几列原先只有文本输入框，现在和其它列一样可以从清单里勾选。
- 候选清单的操作按 Excel 对齐：勾完点「确定」才生效，「取消」保留原来的条件，「清除筛选」去掉整列的筛选；一个都不勾时「确定」是灰的，因为那既不是不筛也不是筛成空。
- 搜索之后勾选默认是替换原来的条件；想在原有条件上追加，勾一下「将当前所选添加到筛选器」。
- 一次勾选超过 1200 项时会当场说明并挡住提交，不再等到点确定之后收到一句没有线索的校验失败。
- 报销单新增「费用归属部门」，表示这笔费用算在哪个部门，不再跟着申请人所在部门走。市场费用、行政费用、研发费用由填报人自己选，可以选任意在用的部门，不限于自己所在的那个；业务部门请市场部的同事去开拓客户，这笔钱就能归到业务部。
- 项目费用的归属部门由所选项目决定，不需要也不能手填；这一格会显示出来但是灰的，旁边写明它由哪里决定。
- 项目费用的归属部门在保存时定下来，之后项目交接给别的部门，已经报过的费用仍然留在原部门，不会被算到新部门头上。
- 报销列表里原来的「部门」列改名为「费用归属部门」，含义同上。
- 新建报销时可以选任何一位在职同事作为申请人，不再限制只能填自己或本部门成员的单子。

修复：

- 新建招投标时漏选「直连项目」没有任何红字提示，得把其它字段都填对了再点一次创建才看得到。现在它和其它必填项一起提示。
- 无法预览的附件会明确说明格式不受支持或无法识别，并提示联系上传人提供可预览版本，不再显示面向开发者的安装提示。
- 按取值重复较多的列排序后往下翻页时，有的记录会出现两次、有的一次都不出现。按审核状态、业务形态、部门这类列排序时最容易遇到。现在每一页的顺序都是确定的。
- 触摸屏上关不掉标签页：关闭的叉号原先只在鼠标悬停时出现，手机和平板上没有悬停，因此一直点不到。报表的模板删除按钮也是同样的问题，一并修复。
- 导航抽屉和标签切换面板打开时，将窗口拉宽会自动关闭面板，不再留下遮罩挡住宽屏界面。
- 归档上传时预填的出具单位取的是同类材料里最早的那一份，不是最近一份；同种类换过出具单位的项目最容易遇到，而界面上看不出这个值是从哪来的。

---

v0.0.2 - 2026-09-17

新增：

- 项目详情页右侧新增「责任归属」卡片，显示当前负责人、所属部门与最近一次交接记录。
- 支持把项目负责人交接给同事。交接不改动项目内容，也不会把审核状态退回草稿，草稿、审核中、已通过的项目都可以交接。跨部门交接时，项目的所属部门会一并转过去。
- 选定接收人后会提示他交接后能不能看到这条项目。这只是提示，不会阻止交接。
- 交接记录可以在卡片上查看：谁交给谁、什么时候、由谁操作。

优化：

- 授权配置页的「配置效果」改成按操作列出，不再按权限位逐条翻译。现在可以直接看到这个人在这类数据上能做哪几件事、每件事的范围是什么。
- 做不了的操作会说明原因并分成三类：缺少权限位、范围没到「全公司」、需要另一类数据的授权（最后一类会标明要切到哪里去配）。
- 少数功能必须把范围配到「全公司」才能使用（餐补月单价、报销的财务字段、系统报表模板），以前配成「本人所属部门」时界面一切正常、功能却始终没有权限，现在会在配置界面直接标出来。
- 受审核状态限制的操作，会一并说明在什么状态下做不了、以及哪一项权限配到「全公司」可以不受限制。
- 选人的下拉框改为按部门分组显示（项目交接的接收人、报销和用车的申请人、招投标经办人、公司制度的可见人员）。现在部门作为分组标题，人名排在它下面，方便按部门查找。搜部门名同样可以找到这个部门的人。
- 报销和用车的申请人下拉不再列出已离职的同事。单据上原本写着的申请人仍然照常显示，只是不能再新选中一个已离职的人。
- 项目类型、费用科目的分类标题不再是一行可以点、可以用键盘跳到的灰色选项，改为真正的分组标题。以前用键盘敲首字母有可能跳到分类上。
- 下拉里当前值不在候选列表时，标注由「（已停用）」改为「（当前不在候选中）」。原先那句话在候选有数量上限或用了搜索时是不准确的，会让人以为某个正常的人或取值被停用了。
- 部门信息取不到时，选人下拉明确提示并支持重试，已取到的人员仍可选择；不会把他们误标为未分配部门。
- 招投标经办人和项目交接下拉因权限不足取不到候选时，直接说明是权限问题，不再提示重试。项目已不存在时也会明确说明。
- 交接接收人被刷新后的候选排除时，保留已选姓名并标注，避免误以为从未选择；此时不能提交无效交接。
- 没有搜索框的短列表也能用方向键选择；加载后出现搜索框时，键盘可以接着输入。清除选择会一并清空搜索词。
- 只有历史取值、没有候选时会明确说明。费用科目名称含斜杠时，选择结果完整保留。
- 用户管理和餐补批量操作完成后保留所选人员与批量入口，方便继续修正同一批人，勾选和计数保持一致。
- 审核通过或退回后只取消本次处理记录的选择，保留后来另选的记录；提交后切换标签也不会把已处理记录重新选中。
- 公司制度列表统一通过「文件」列的文件名预览普通文件或浏览包内文件，操作列只保留编辑名称与类型、调整可见范围、废止与恢复。
- 列表和报表里大部分列的筛选同时提供搜索框和可勾选的取值清单：可以输入关键词匹配，也可以直接从清单里勾选，两边一起用时按同时满足处理。
- 审核状态、是否归档、中标情况这类列仍然只提供勾选清单，因为表格里显示的文字与系统里存的取值不是同一个词，按显示的文字搜索会搜不到；报销金额这类取值成千上万的列仍然只提供搜索框。
- 合同列表的税率筛选框注明按小数填写（6% 填 0.06），与格子里显示的百分数区分开。
- 以前保存的表格视图和报表模板照常可用，其中的筛选条件会自动落到新的筛选框里。

修复：

- 应用报表模板时保留已保存的筛选取值，不再因之前打开过候选清单而变成空结果。
- 筛选清单里的空白项显示为「(空白)」，与其它列的写法一致，不再出现没有名称、看不出是什么的选项。
- 公司制度「可见范围」列的筛选清单显示「全员可见」「限定可见」，不再列出系统内部的英文代号。
- 状态类列的筛选下拉正确区分「是/否」「有效/已废止」「在职/已停用」等选项，不再出现两个同名选项；筛选摘要与表格显示保持一致。
- 文本列的「空白」筛选统一包含未填写、空字符串和全空白字符的记录。

---

v0.0.1 - 2026-09-15

迁移：

- 完整迁移老版本到新项目管理软件。
